<div class="container text-center include-navbar">
  <h2>New Question</h2>

  <%= simple_form_for([@user, @question]) do |f| %>
    <%= f.input :content %>
    <%= f.input :answer %>
    <%= f.button :submit %>
  <% end %>
</div>
